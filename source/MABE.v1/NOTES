This initial attempt at re-writing MABE focused on building worlds from template parameters that
all plugged together.  Ditching this attempt (perhaps only temporarily) to try for something
more flexible where all interactions occur through base classes.
